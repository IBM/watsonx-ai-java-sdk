<?xml version="1.0" encoding="UTF-8"?>
<project
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd"
  xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>
  <name>watsonx-ai</name>
  <artifactId>watsonx-ai</artifactId>
  <url>https://github.com/IBM/watsonx-ai-java-sdk</url>

  <scm>
    <connection>scm:git:git://github.com/IBM/watsonx-ai-java-sdk.git</connection>
    <developerConnection>scm:git:ssh://github.com/IBM/watsonx-ai-java-sdk.git</developerConnection>
    <url>https://github.com/IBM/watsonx-ai-java-sdk</url>
  </scm>

  <description>
  Service layer implementation of the watsonx.ai SDK. This module contains the service 
  classes to interact directly with the watsonx.ai platform
  </description>

  <parent>
    <groupId>com.ibm.watsonx</groupId>
    <artifactId>watsonx-ai-parent</artifactId>
    <version>999-SNAPSHOT</version>
    <relativePath>../..</relativePath>
  </parent>

  <dependencies>
    <dependency>
      <groupId>com.ibm.watsonx</groupId>
      <artifactId>watsonx-ai-core</artifactId>
      <version>999-SNAPSHOT</version>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jboss.weld</groupId>
      <artifactId>weld-junit5</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>build-helper-maven-plugin</artifactId>
        <version>${build-helper-maven-plugin-version}</version>
        <executions>
          <execution>
            <id>add-sharedtest-source</id>
            <phase>generate-test-sources</phase>
            <goals>
              <goal>add-test-source</goal>
            </goals>
            <configuration>
              <sources>
                <source>src/sharedtest/java</source>
              </sources>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <executions>
          <!--
            src/sharedtest/java is registered as an extra test source root above, so the
            default testCompile execution already compiles it into target/test-classes
            (needed for watsonx-ai's own Surefire run). This second execution recompiles the
            same classes on their own into target/sharedtest-classes, so maven-jar-plugin's
            test-jar execution below can package only them, without an excludes list.
          -->
          <execution>
            <id>sharedtest-testCompile</id>
            <phase>process-test-classes</phase>
            <goals>
              <goal>testCompile</goal>
            </goals>
            <configuration>
              <compileSourceRoots>
                <compileSourceRoot>${project.basedir}/src/sharedtest/java</compileSourceRoot>
              </compileSourceRoots>
              <outputDirectory>${project.build.directory}/sharedtest-classes</outputDirectory>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>${maven-jar-plugin-version}</version>
        <executions>
          <execution>
            <goals>
              <goal>test-jar</goal>
            </goals>
            <configuration>
              <testClassesDirectory>${project.build.directory}/sharedtest-classes</testClassesDirectory>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>native-agent</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-compiler-plugin</artifactId>
            <configuration>
              <testExcludes>
                <testExclude>**/ContextDepedencyInjectionTest.java</testExclude>
                <testExclude>**/CustomRestClientTest.java</testExclude>
              </testExcludes>
            </configuration>
          </plugin>
          <plugin>
            <groupId>org.graalvm.buildtools</groupId>
            <artifactId>native-maven-plugin</artifactId>
            <configuration>
              <agent>
                <enabled>true</enabled>
                <options>
                  <callerFilterFiles>
                    <filterFile>${project.basedir}/native-agent-filter.json</filterFile>
                  </callerFilterFiles>
                  <accessFilterFiles>
                    <filterFile>${project.basedir}/native-agent-filter.json</filterFile>
                  </accessFilterFiles>
                </options>
                <metadataCopy>
                  <merge>false</merge>
                  <outputDirectory>${project.basedir}/src/main/resources/META-INF/native-image/com.ibm.watsonx/${project.artifactId}</outputDirectory>
                  <disabledStages>
                    <stage>main</stage>
                  </disabledStages>
                </metadataCopy>
              </agent>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>

</project>